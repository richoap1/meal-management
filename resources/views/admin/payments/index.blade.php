@extends('layouts.app')
@section('content')
<div class="flex flex-col gap-6"><div><p class="text-sm font-semibold text-emerald-700">ADMIN / BILLING</p><h1 class="text-3xl font-bold text-gray-900">Payment review</h1><p class="mt-1 text-sm text-gray-500">Verify bank transfers and QRIS submissions before activating membership.</p></div>@if(session('success'))<div class="rounded-lg bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif<div class="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-gray-50 text-xs uppercase text-gray-500"><tr><th class="px-5 py-3">Invoice / user</th><th class="px-5 py-3">Method</th><th class="px-5 py-3">Amount</th><th class="px-5 py-3">Proof</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Review</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($payments as $payment)<tr><td class="px-5 py-4"><p class="font-semibold text-gray-900">{{ $payment->invoice_number }}</p><p class="text-xs text-gray-500">{{ $payment->user->name }} · {{ $payment->user->email }}</p></td><td class="px-5 py-4">{{ $payment->method === 'qris' ? 'QRIS' : 'Bank transfer' }}@if($payment->qris_reference)<p class="text-xs text-gray-400">{{ $payment->qris_reference }}</p>@endif</td><td class="px-5 py-4 font-semibold">Rp {{ number_format($payment->amount, 0, ',', '.') }}</td><td class="px-5 py-4">@if($payment->proof_path)<a target="_blank" href="{{ route('admin.payments.proof', $payment) }}" class="text-xs font-semibold text-emerald-700">View proof</a>@else<span class="text-xs text-gray-400">QRIS check</span>@endif</td><td class="px-5 py-4"><span class="rounded-full px-2 py-1 text-xs font-semibold {{ $payment->status === 'approved' ? 'bg-emerald-50 text-emerald-700' : ($payment->status === 'rejected' ? 'bg-red-50 text-red-700' : 'bg-amber-50 text-amber-700') }}">{{ ucfirst($payment->status) }}</span></td><td class="px-5 py-4 text-right">@if($payment->status === 'pending')<form method="POST" action="{{ route('admin.payments.update', $payment) }}" class="flex justify-end gap-2">@csrf @method('PUT')<input type="hidden" name="status" value="approved"><button class="rounded-lg bg-emerald-700 px-3 py-2 text-xs font-semibold text-white">Approve</button></form><form method="POST" action="{{ route('admin.payments.update', $payment) }}" class="mt-2 flex justify-end gap-2">@csrf @method('PUT')<input type="hidden" name="status" value="rejected"><input name="admin_note" placeholder="Reason (optional)" class="w-36 rounded-lg border border-gray-200 px-2 py-1 text-xs"><button class="rounded-lg border border-red-200 px-3 py-2 text-xs font-semibold text-red-600">Reject</button></form>@else<span class="text-xs text-gray-400">Reviewed {{ $payment->reviewed_at?->format('d M Y H:i') }}</span>@endif</td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-sm text-gray-500">No payment submissions yet.</td></tr>@endforelse</tbody></table></div></div></div>
@endsection