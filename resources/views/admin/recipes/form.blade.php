@extends('layouts.app')
@section('content')
<div class="mx-auto max-w-3xl"><a href="{{ route('admin.recipes.index') }}" class="text-sm font-semibold text-emerald-700">← Back to recipes</a><div class="mt-4 rounded-xl border border-gray-200 bg-white p-6 shadow-sm"><h1 class="text-2xl font-bold text-gray-900">{{ $meal->exists ? 'Edit recipe' : 'Add recipe' }}</h1><p class="mt-1 text-sm text-gray-500">Add measured ingredients and clear cooking instructions.</p><form class="mt-6 space-y-5" method="POST" action="{{ $meal->exists ? route('admin.recipes.update', $meal) : route('admin.recipes.store') }}" enctype="multipart/form-data">@csrf @if($meal->exists) @method('PUT') @endif
<label class="block text-sm font-semibold text-gray-700">Recipe name<input name="name" value="{{ old('name', $meal->name) }}" required class="mt-2 w-full rounded-lg border border-gray-300 px-3 py-2.5 font-normal"></label><label class="block text-sm font-semibold text-gray-700">Description<textarea name="description" rows="2" class="mt-2 w-full rounded-lg border border-gray-300 px-3 py-2.5 font-normal">{{ old('description', $meal->description) }}</textarea></label><div class="grid gap-5 sm:grid-cols-3"><label class="block text-sm font-semibold text-gray-700">Meal type<select name="type" class="mt-2 w-full rounded-lg border border-gray-300 px-3 py-2.5 font-normal">@foreach(['breakfast','lunch','dinner'] as $type)<option value="{{ $type }}" {{ old('type', $meal->type ?: 'lunch') === $type ? 'selected' : '' }}>{{ ucfirst($type) }}</option>@endforeach</select></label><label class="block text-sm font-semibold text-gray-700">Calories<input name="calories" type="number" min="0" max="10000" value="{{ old('calories', $meal->calories) }}" required class="mt-2 w-full rounded-lg border border-gray-300 px-3 py-2.5 font-normal"></label><label class="block text-sm font-semibold text-gray-700">Price<input name="price" type="number" min="0" step="100" value="{{ old('price', $meal->price ?? 0) }}" class="mt-2 w-full rounded-lg border border-gray-300 px-3 py-2.5 font-normal"></label></div><label class="block text-sm font-semibold text-gray-700">Ingredients<textarea name="ingredients" rows="6" required placeholder="75 g rice&#10;100 g chicken&#10;100 g vegetables" class="mt-2 w-full rounded-lg border border-gray-300 px-3 py-2.5 font-normal">{{ old('ingredients', $meal->ingredients) }}</textarea><span class="mt-1 block text-xs font-normal text-gray-400">One ingredient per line, including its quantity.</span></label><label class="block text-sm font-semibold text-gray-700">Cooking instructions<textarea name="instructions" rows="6" required placeholder="Wash and prepare all ingredients.&#10;Cook until done.&#10;Serve warm." class="mt-2 w-full rounded-lg border border-gray-300 px-3 py-2.5 font-normal">{{ old('instructions', $meal->instructions) }}</textarea><span class="mt-1 block text-xs font-normal text-gray-400">One step per line.</span></label><label class="block text-sm font-semibold text-gray-700">Recipe image<input name="image" type="file" accept="image/jpeg,image/png,image/webp" class="mt-2 block w-full rounded-lg border border-gray-300 px-3 py-2 font-normal"></label><label class="flex items-center gap-2 text-sm font-semibold text-gray-700"><input name="is_available" type="checkbox" value="1" {{ old('is_available', $meal->exists ? $meal->is_available : true) ? 'checked' : '' }} class="rounded border-gray-300 text-emerald-700"> Available for users</label><button class="w-full rounded-lg bg-emerald-700 px-4 py-3 text-sm font-semibold text-white hover:bg-emerald-800">{{ $meal->exists ? 'Save changes' : 'Create recipe' }}</button></form></div></div>
@endsection