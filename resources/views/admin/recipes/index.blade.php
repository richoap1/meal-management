@extends('layouts.app')
@section('content')
<div class="flex flex-col gap-6"><div class="flex flex-wrap items-center justify-between gap-4"><div><p class="text-sm font-semibold text-emerald-700">ADMIN / CONTENT</p><h1 class="text-3xl font-bold text-gray-900">Recipe library</h1><p class="mt-1 text-sm text-gray-500">Create recipes that users can discover and add to their plans.</p></div><a href="{{ route('admin.recipes.create') }}" class="rounded-lg bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white hover:bg-emerald-800">+ Add recipe</a></div>
@if(session('success'))<div class="rounded-lg bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
<div class="grid gap-5 md:grid-cols-2 lg:grid-cols-3">@forelse($meals as $meal)<article class="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm"><div class="h-44 bg-gray-100">@if($meal->image_path)<img src="{{ asset('storage/'.$meal->image_path) }}" alt="{{ $meal->name }}" class="h-full w-full object-cover">@else<div class="flex h-full items-center justify-center text-sm text-gray-400">No recipe image</div>@endif</div><div class="p-5"><div class="flex justify-between gap-3"><span class="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold capitalize text-emerald-700">{{ $meal->type }}</span><span class="text-xs font-semibold text-gray-500">{{ $meal->calories ?? 0 }} kcal</span></div><h2 class="mt-3 font-bold text-gray-900">{{ $meal->name }}</h2><p class="mt-2 line-clamp-2 text-sm text-gray-500">{{ $meal->description }}</p><div class="mt-5 flex gap-3"><a href="{{ route('admin.recipes.edit', $meal) }}" class="text-xs font-semibold text-emerald-700">Edit</a><form method="POST" action="{{ route('admin.recipes.destroy', $meal) }}" onsubmit="return confirm('Delete this recipe?')">@csrf @method('DELETE')<button class="text-xs font-semibold text-red-600">Delete</button></form></div></div></article>@empty<p class="rounded-xl border border-dashed border-gray-300 bg-white p-10 text-center text-sm text-gray-500 md:col-span-2 lg:col-span-3">No recipes yet. Add the first one.</p>@endforelse</div></div>
@endsection