@extends('layouts.app')
@section('content')
<div class="mx-auto max-w-2xl">
    <a href="{{ route('admin.stores.products', $store) }}" class="text-sm font-semibold text-emerald-700">← Back to products</a>
    <div class="mt-4 rounded-xl border border-gray-200 bg-white p-6 shadow-sm"><h1 class="text-2xl font-bold text-gray-900">{{ $product->exists ? 'Edit product' : 'Add product' }}</h1>
    <p class="mt-1 text-sm text-gray-500">Product for {{ $store->name }}.</p><form class="mt-6 space-y-5" method="POST" action="{{ $product->exists ? route('admin.stores.products.update', [$store, $product]) : route('admin.stores.products.store', $store) }}" enctype="multipart/form-data">@csrf @if($product->exists) @method('PUT') @endif
<label class="block text-sm font-semibold text-gray-700">Product name<input name="product_name" value="{{ old('product_name', $product->product_name) }}" required class="mt-2 w-full rounded-lg border border-gray-300 px-3 py-2.5 font-normal"></label><div class="grid gap-5 sm:grid-cols-2"><label class="block text-sm font-semibold text-gray-700">Price<input name="price" type="number" min="0" step="100" value="{{ old('price', $product->price) }}" required class="mt-2 w-full rounded-lg border border-gray-300 px-3 py-2.5 font-normal"></label><label class="block text-sm font-semibold text-gray-700">Category<select name="category" required class="mt-2 w-full rounded-lg border border-gray-300 px-3 py-2.5 font-normal">@foreach(['Karbohidrat','Protein','Sayuran'] as $category)<option {{ old('category', $product->category) === $category ? 'selected' : '' }}>{{ $category }}</option>@endforeach</select></label></div><label class="block text-sm font-semibold text-gray-700">Product image<input name="image" type="file" accept="image/jpeg,image/png,image/webp" class="mt-2 block w-full rounded-lg border border-gray-300 px-3 py-2 font-normal"></label><label class="flex items-center gap-2 text-sm font-semibold text-gray-700"><input name="is_available" type="checkbox" value="1" {{ old('is_available', $product->exists ? $product->is_available : true) ? 'checked' : '' }} class="rounded border-gray-300 text-emerald-700"> Available for meal planning</label><button class="w-full rounded-lg bg-emerald-700 px-4 py-3 text-sm font-semibold text-white">{{ $product->exists ? 'Save changes' : 'Create product' }}</button></form></div></div>
@endsection