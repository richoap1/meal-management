@extends('layouts.app')
@section('content')
<div class="mx-auto max-w-6xl space-y-7"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-semibold tracking-widest text-emerald-700">MEALWISE / RECIPE LIBRARY</p><h1 class="mt-2 text-3xl font-bold text-gray-900">Recipes for every kind of day</h1><p class="mt-1 text-sm text-gray-500">Browse complete recipes with measured ingredients and simple cooking steps.</p></div><a href="{{ route('planner.index') }}" class="rounded-lg bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white hover:bg-emerald-800">Build a meal plan</a></div>
<div class="flex flex-wrap gap-2"><button class="recipe-filter rounded-full bg-emerald-700 px-4 py-2 text-xs font-semibold text-white" data-filter="all">All recipes</button><button class="recipe-filter rounded-full border border-gray-200 bg-white px-4 py-2 text-xs font-semibold text-gray-600" data-filter="Diet">Diet</button><button class="recipe-filter rounded-full border border-gray-200 bg-white px-4 py-2 text-xs font-semibold text-gray-600" data-filter="Non-diet">Non-diet</button></div>
<div class="grid gap-5 md:grid-cols-2 lg:grid-cols-3">@foreach($recipes as $recipe)<article class="recipe-library-card overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm" data-mode="{{ $recipe['mode'] }}"><img src="{{ $recipe['image'] }}" alt="{{ $recipe['title'] }}" class="h-48 w-full object-cover" loading="lazy"><div class="p-5"><div class="flex items-center justify-between gap-3"><span class="rounded-full {{ $recipe['mode'] === 'Diet' ? 'bg-lime-100 text-lime-800' : 'bg-emerald-50 text-emerald-700' }} px-2.5 py-1 text-xs font-semibold">{{ $recipe['mode'] }}</span><span class="text-xs font-semibold text-gray-500">{{ $recipe['type'] }} · {{ $recipe['calories'] }} kcal</span></div><h2 class="mt-3 text-lg font-bold capitalize text-gray-900">{{ $recipe['title'] }}</h2><h3 class="mt-5 text-xs font-bold uppercase tracking-wider text-gray-500">Ingredients · 1 serving</h3><ul class="mt-2 list-disc space-y-1 pl-5 text-sm text-gray-600">@foreach($recipe['ingredients'] as $ingredient)<li>{{ $ingredient }}</li>@endforeach</ul><h3 class="mt-5 text-xs font-bold uppercase tracking-wider text-gray-500">How to cook</h3><ol class="mt-2 list-decimal space-y-1 pl-5 text-sm leading-6 text-gray-600">@foreach($recipe['steps'] as $step)<li>{{ $step }}</li>@endforeach</ol></div></article>@endforeach</div></div>
@endsection
@push('scripts')<script>document.querySelectorAll('.recipe-filter').forEach((button)=>button.addEventListener('click',()=>{document.querySelectorAll('.recipe-filter').forEach((item)=>item.classList.remove('bg-emerald-700','text-white'));button.classList.add('bg-emerald-700','text-white');document.querySelectorAll('.recipe-library-card').forEach((card)=>{card.hidden=button.dataset.filter!=='all'&&card.dataset.filter!==button.dataset.filter&&card.dataset.filter!==card.dataset.mode;});}));</script>@endpush